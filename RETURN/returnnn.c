#define t3t4 w 
void main(){
    char w;
    double asin , acos;
        float t3, t4;
}
struct rect t3t4(){
    struct rect t3();
    struct rect t4();
}
void returnnn(){
        return f / asin(w + t3) + h / asin(w - t4)
        return h / acos(w + t3) + f / acos(w - t4)
        return f / asin(w + t3) + h / asin(w + t4)
        return h / acos(w - t3) + f / acos(w - t4)
}
